[
  {
    "description": "List PIDs of processes matching a name pattern",
    "command": "pgrep <pattern>",
    "params": {
      "pattern": {
        "description": "Process name regex"
      }
    }
  },
  {
    "description": "Match against the full command line, not just the name",
    "command": "pgrep -f <pattern>",
    "params": {
      "pattern": {
        "type": "string",
        "description": "Regex matched against the full command line"
      }
    }
  },
  {
    "description": "Kill every process matching a pattern (use with care)",
    "command": "pgrep -f <pattern> | xargs kill",
    "params": {
      "pattern": {
        "type": "string",
        "description": "Regex matched against the full command line"
      }
    }
  },
  {
    "description": "Show name and PID for matches",
    "command": "pgrep -l <pattern>",
    "params": {
      "pattern": {
        "description": "Process name regex"
      }
    }
  }
]
