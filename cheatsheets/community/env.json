[
  {
    "description": "List exported environment variables and values",
    "command": "env"
  },
  {
    "description": "Set an environment variable and run a command",
    "command": "env <variable>=<value> <command>",
    "params": {
      "variable": {
        "description": "Variable name"
      },
      "value": {
        "type": "string",
        "description": "Value to set"
      },
      "command": {
        "type": "string",
        "description": "Command to run with the variable set"
      }
    }
  },
  {
    "description": "Remove an environment variable and run a command",
    "command": "env -u <variable> <command>",
    "params": {
      "variable": {
        "description": "Variable name to unset"
      },
      "command": {
        "type": "string",
        "description": "Command to run"
      }
    }
  },
  {
    "description": "Clear the environment entirely and run a command",
    "command": "env -i <command>",
    "params": {
      "command": {
        "type": "string",
        "description": "Command to run with an empty environment"
      }
    }
  }
]
